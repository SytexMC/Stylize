name: Stylize
version: '2.1.0'
api-version: '1.20.6'
main: me.sytex.stylize.Plugin
authors: [ SytexMC ]
contributors: [ ]
description: "A library for formatting and sending text using Adventure's MiniMessage format, as well as legacy formats."
dependencies:
  server:
    PlaceholderAPI:
      load: BEFORE
      required: false
      join-classpath: true
    MiniPlaceholders:
      load: BEFORE
      required: false
      join-classpath: true